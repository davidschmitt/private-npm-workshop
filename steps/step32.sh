#
#
# Build and run the sample app from the second package
#
# Follow the instructions provided to sign into the NPM registry
# using the consumer credentials.
#
docker run -it --net vrdnet pkg2image:latest
